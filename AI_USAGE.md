# AI_USAGE

과제 진행에 AI Agent를 사용한 내역. `spec/requirement.md`의 제출 항목 기준으로 작성한다.
기록은 커밋 시점에 함께 쌓고, 끝에 재구성하지 않는다.

## 사용한 도구/모델

- Claude Code (CLI), 모델: Claude Fable 5, 플러그인: SuperClaude, oh-my-clodecode
- 작업 규약과 계획 문서는 `CLAUDE.md` 참고
- 용도: 계획 논의, 문서 작성, 스캐폴드 구성, 구현과 테스트 작성 위임.
  모든 결과물은 사람이 리뷰 후 커밋

## 적용한 작업 범위

| 영역 | AI 관여 | 비고 |
| --- | --- | --- |
| 프로젝트 규약 문서 (CLAUDE.md) | 논의 후 작성 위임 | 방침 결정은 사람, 사람이 검토 후 커밋 |
| 커밋 전략, 문서 구조 설계 | 논의 | 최종 결정은 사람 |
| 도구 체인 스캐폴드 (Vite, Tailwind, ESLint, Vitest) | 구성 위임 | 사람이 게이트 실행으로 검증 |
| MSW 목 서버 (핸들러, 시드, 인증 목) | 구현과 테스트 작성 위임 | refresh 토큰 전달 방식은 사람이 결정 |
| http 클라이언트, 가상 스크롤 (red/green) | 테스트와 구현 위임 | 설계 선택지는 문답으로 사람이 결정 |
| 라우터 셸, GNB, 페이지 골격 | 구현과 테스트 작성 위임 | 내비 형태와 비로그인 정책은 사람이 결정 |
| 디자인 토큰 체계, GNB 스타일 | 구현 위임 | 방향과 세부 조정은 사람이 스크린샷 보며 지시 |
| 로그인 폼, 공용 UI (Button, Input, Modal), 세션 | 구현과 테스트 작성 위임 | 비밀번호 규칙 해석은 사람이 결정 |
| 대시보드 (TanStack Query 도입) | 구현과 테스트 작성 위임 | Query 재시도 정책은 http 클라이언트와의 역할 분리로 결정 |
| 할 일 상세 조회 (404 전용 화면) | 구현과 테스트 작성 위임 | 커밋 분할 기준은 사람이 재검토해 조정 |
| 부팅 시 세션 복원 | 원인 규명과 구현, 테스트 작성 위임 | 범위 편입과 작업 순서는 사람이 결정 |
| 삭제 확인 모달, 삭제 후 안내 | 구현과 테스트 작성 위임 | 삭제 후 UX와 계약 유지 방침은 사람이 결정 |
| 목록 스크롤 위치 복원 | 구현과 테스트 작성 위임 | 복원 범위를 사람이 재검토해 방향 전환 |
| 회원정보 화면 | 구현과 테스트 작성 위임 | 스펙 공백이 없어 기존 화면 패턴 재사용 |
| 로그아웃 | 구현과 테스트 작성 위임 | 요구사항 밖 기능. 편입 근거와 배치는 사람이 결정 |

구현이 진행되면 영역별로 행을 추가한다.

## 핵심 프롬프트 요약

커밋 단위로 기록한다. 커밋 제목이 아래 검증 항목과의 연결 키.

- `docs: 과제 요구사항과 API 계약 추가`: 제공 파일 파악과 진행 상태 분석 요청.
  파일 자체는 회사 제공 원본이라 AI 산출물 없음
- `chore: 프로젝트 규약과 AI_USAGE 뼈대 추가`: CLAUDE.md 초안을 놓고 문서 구조
  (README와 AI_USAGE의 역할 경계), 커밋 슬라이스 전략, FSD 세그먼트 규약을
  논의하고 반영 요청
- `docs: 제공 문서 spec 디렉토리로 이동`: 제공 문서 격리 배치를 제안하고
  장단점 검토 요청, 전체 디렉토리 구조안 작성 요청
- `chore: Vite React TypeScript 스캐폴드 구성`: 비어 있지 않은 디렉토리라
  템플릿 생성기 대신 파일 직접 작성 위임. TS 7에서 baseUrl 제거 이슈를
  AI가 수정
- `build: Tailwind 토큰과 Pretendard 폰트 구성`: 색상 토큰 세트 선정과
  전역 스타일 작성 위임. 토큰 명명은 requirement 예시(primary, disabled)
  기준으로 지시
- `chore: ESLint FSD 레이어 경계 규칙 구성`: eslint-plugin-boundaries로
  CLAUDE.md의 레이어 규칙을 lint로 강제하는 설정 위임. v7 문법 마이그레이션은
  공식 문서 확인 후 진행
- `chore: Vitest와 openapi 타입 생성 파이프라인 구성`: Vitest, Testing Library
  셋업과 openapi-typescript 기반 gen:api 스크립트 구성 위임
- `feat: MSW 목 서버와 시드 추가`: openapi.yaml 7개 엔드포인트의 핸들러,
  시드 500건, 가짜 JWT 발급과 만료 검사, 핸들러 계약 테스트 14건 작성 위임.
  httpOnly 쿠키 제약에서 refresh 토큰 전달 방식(스펙 명세대로 일반 쿠키
  token 사용)을 논의로 결정
- `test: 401 토큰 갱신과 재시도 스펙 추가`: http 클라이언트 설계(직접 구현,
  콜백 주입, 반응형 갱신만)를 3개 선택지 문답으로 확정한 뒤, 합의한 동작
  규약 7건을 실패하는 테스트로 작성 위임. 테스트 파일의 목 서버 접근을 위한
  lint 예외 정책 추가 포함
- `feat: 401 토큰 갱신과 원요청 재시도 구현`: red 스펙 7건을 통과시키는
  구현 위임. single-flight는 refresh promise 공유와 완료 시 해제,
  세션 만료 통지는 refresh 실행 단위로 1회 발화하는 구조
- `test: 가상 스크롤 렌더 윈도우 스펙 추가`: 설계 문답으로 범위(순수 계산 +
  훅)와 갱신 전략(윈도우 변화 시에만 setState)을 확정한 뒤, 경계 케이스와
  렌더 수 상한 속성 검사를 포함한 실패 테스트 12건 작성 위임
- `feat: 가상 스크롤 렌더 윈도우 계산과 훅 구현`: red 스펙 12건을 통과시키는
  구현 위임. 스크롤 위치는 ref 보관, 스크롤마다 전후 윈도우를 계산해
  달라졌을 때만 리렌더하는 구조
- `feat: 라우터 셸과 GNB 추가`: 내비 형태(상단 GNB)와 비로그인 정책(로그인
  리다이렉트)을 문답으로 정한 뒤 코드 기반 라우트 트리, 인증 가드, GNB 위젯,
  페이지 골격 5개와 동작 테스트 4건 작성 위임. 화면 규칙을 CLAUDE.md에,
  지금까지의 설계 결정을 README에 문서화
- `feat: StyleSeed 규칙 기반 디자인 토큰 체계 도입`: 스타일 방향을 논의로
  결정 (StyleSeed 오픈소스의 규칙 체계만 가져오고 팔레트는 자체 구성,
  Toss 스킨 색 미사용, 라이트 온리). 토큰 교체, 잠금 파일 작성, 기존 화면
  재정돈 위임
- `feat: 로그인 폼과 세션 추가`: RHF와 zod 폼, 공용 UI 3종(Button, Input,
  Modal 포커스 트랩), 리액티브 세션 스토어, redirect 복귀 연결과 테스트
  8건 작성 위임. 구현 후 브라우저에서 로그인 전 과정을 에이전트가 조작해
  사전 확인. 사람이 발견한 카드 좌상단 렌더 이상을 radius 토큰명과 Tailwind
  내장 rounded-ss(좌상단 코너 유틸)의 충돌로 진단하고 rounded-card로 개명,
  로그인 카드 세로 중앙 배치 조정
- `feat: 대시보드 지표 표시 추가`: TanStack Query 도입(Query 재시도는 http
  클라이언트의 401 재시도와 역할 분리로 끔), 지표 카드 3개와 로딩 스켈레톤,
  에러 상태와 다시 시도, 테스트 2건 작성 위임. 잠금 파일의 상태 실렌더
  규칙 첫 적용
- `feat: 할 일 목록과 무한 스크롤 추가`: useInfiniteQuery 목록, 고정 높이
  TaskCard와 상태 뱃지, 스크롤 끝 접근 시 다음 페이지, 빈/에러 상태와
  테스트 5건 작성 위임. 에이전트가 브라우저 확인 중 body 스크롤 때문에
  내부 컨테이너의 무한 스크롤 트리거가 동작하지 않는 버그를 발견,
  레이아웃을 뷰포트 고정(h-dvh) 방식으로 수정
- `feat: 목록에 가상 스크롤 적용`: useVirtualWindow를 목록에 연결하는 작업
  위임. 이중 컨테이너(전체 높이 + translateY)로 스크롤 중 reflow 회피,
  윈도우 밖 항목은 DOM에서 제거. 테스트를 윈도우 렌더 기준으로 갱신.
  에이전트가 브라우저에서 DOM 노드 16개 유지와 무한 스크롤 병행 동작을
  사전 확인. 구현 후 리뷰어 서브에이전트로 교차 리뷰를 돌려 셀프 리뷰가
  놓친 재마운트 시 scrollTop 어긋남 버그(높음 1건)와 접근성 3건 등
  15건을 받았고, 10건 반영 (나머지는 트레이드오프로 README 한계에 기록)
- `feat: 할 일 상세 조회 추가`: 상세 슬라이스 설계 문답(삭제 확인 input의
  비교 기준, 삭제 후 UX, 목록 스크롤 위치 복원 여부) 후 상세 조회 화면
  구현과 테스트 3건 작성 위임. 404는 다시 시도해도 결과가 같으므로
  목록 복귀만 제공하는 전용 화면으로 분기. 커밋 계획은 사람이 뭉텅이가
  크다고 지적해 2개에서 3개로 재분할
- `feat: 부팅 시 세션 복원 추가`: 새로고침과 딥링크에서 로그인이 풀리는 증상의
  원인 규명과 수정 위임. refresh 요청을 순수 함수로 분리해 401 갱신과 부팅
  복원이 같은 요청을 쓰되 실패 처리만 다르게 두는 구조. 부팅 실패를 세션
  만료로 통지하면 안 되는 이유를 테스트로 잠그는 것까지 포함해 4건 작성 위임
- `feat: 할 일 삭제 확인 모달 추가`: 확인 input이 라우트 파라미터와 일치할 때만
  제출이 열리는 모달, 삭제 mutation과 캐시 무효화, 목록 복귀 후 안내까지 구현과
  테스트 5건 작성 위임. feature가 라우터를 모르도록 완료를 콜백으로 알리는 구조.
  상세 응답에 id와 status가 없는 것을 두고 제공된 openapi.yaml에 필드를 추가할지
  물었고, 계약을 고치지 않고 판단을 문서에 남기는 쪽으로 사람이 결정.
  삭제 안내는 처음 인라인 배너로 만들었다가 사람이 상단 중앙 토스트로 지시해 교체.
  shadcn 같은 컴포넌트 모음을 들이지 않고 포털과 라이브 리전으로 직접 구현
- `feat: 상세에 목록 복귀 버튼 추가`: 상세에서 목록으로 돌아갈 수단이 브라우저
  뒤로가기뿐이라는 사람의 지적에서 시작. 세션 복원 이후 딥링크 진입이 가능해져
  히스토리가 비어 있을 수 있다는 점을 근거로 범위에 편입. 배치와 모양은 사람이
  세 번 조정 지시 (제목 위 텍스트 링크에서 삭제 버튼 좌측으로, 링크에서 ghost
  버튼으로, 아이콘은 ArrowLeft에서 ChevronLeft로)
- `feat: 목록 스크롤 위치 복원 추가`: 처음에는 모듈 변수로 모든 진입에서 복원하는
  안을 냈으나, 사람이 메뉴로 새로 들어오면 처음부터 보여주는 게 맞지 않냐고 되물어
  재검토. 브라우저 기본 동작과 라우터들의 규약이 히스토리 항목 기준이라는 근거를
  확인하고 방향을 바꿈. 공식 문서 확인 후 라우터 내장 스크롤 복원을 쓰고, 가상
  스크롤 훅에 초기 오프셋 옵션을 더하는 구현과 테스트 2건 작성 위임
- `feat: 회원정보 표시 추가`: `GET /api/user`의 name과 memo를 보여주는 화면과
  로딩, 에러 상태, 테스트 2건 작성 위임. 스펙 공백도 새 결정도 없어 대시보드와
  상세에서 쓰던 상태 분기와 타이포 규칙을 그대로 재사용
- `feat: 로그아웃 추가`: 사람이 평가 편의를 이유로 필요성을 물었고, 부팅 복원을
  넣은 뒤로 명세된 비로그인 GNB 상태에 닿을 방법이 사라졌다는 근거를 확인해
  범위에 편입. 배치는 사람이 상세의 삭제 버튼과 같은 자리로 지정.
  세션 종료와 쿼리 캐시 비우기, 테스트 1건 작성 위임

## 사람이 최종 검증한 내용

커밋 단위로 기록한다. AI 제안을 반려하거나 방향을 바꾼 경우도 여기에 남긴다.

- `docs: 과제 요구사항과 API 계약 추가`: 제공 파일이 원본 그대로인지 확인 후 커밋
- `chore: 프로젝트 규약과 AI_USAGE 뼈대 추가`: CLAUDE.md 수정본을 직접 검토 후 승인.
  이 과정에서 방향을 바꾼 결정 두 건
    1. 설계 결정을 별도 `DECISIONS.md`로 두자는 제안을 반려하고 README의 설계 결정 섹션으로 통합. 평가자가 반드시 읽는 위치를 우선
    2. 코어 로직을 사람이 직접 구현하는 소유권 구분을 제거하고 전면 위임으로 전환. 대신 커밋 단위 리뷰와 검증 기록을 강화
- `docs: 제공 문서 spec 디렉토리로 이동`: git rename 추적과 문서 내 경로 참조
  갱신 확인. FSD 디렉토리 구조안(코드 기반 라우팅, 생성 타입 예외, mocks
  레이어 밖 배치, 회원정보 경로 /user)을 검토 후 승인
- `chore: Vite React TypeScript 스캐폴드 구성`: typecheck와 build 통과 확인
- `build: Tailwind 토큰과 Pretendard 폰트 구성`: 빌드 산출 CSS에 토큰과
  폰트 포함 확인, 텍스트 색 명암비 4.5:1 이상 확인
- `chore: ESLint FSD 레이어 경계 규칙 구성`: 위반 케이스 3종(하위가 상위
  import, 같은 레이어 슬라이스 간 import, index.ts 우회 깊은 경로 import)이
  실제로 잡히는지 임시 파일로 확인. typescript-eslint가 TS 7 미지원이라
  TS 6 고정 결정
- `chore: Vitest와 openapi 타입 생성 파이프라인 구성`: 임시 스모크 테스트로
  jsdom과 RTL 렌더 경로 동작 확인 후 제거, 생성 타입이 typecheck와 lint를
  통과하는지 확인. 완료 기준 3종 게이트(typecheck, lint, test:run) 전부 동작
- `feat: MSW 목 서버와 시드 추가`: 
    1. 3종 게이트와 핸들러 계약 테스트 통과 확인. 개발자 도구에서 직접 호출로 로그인 성공.
    비밀번호 오류 시 400 상태와 errorMessage, 토큰 없는 보호 API의 401 상태 확인.
    2. refresh 토큰을 localStorage에 두기로 했던 설계를 재검토하고, openapi
    명세의 쿠키 계약(이름 token)을 그대로 따르는 일반 쿠키 방식으로 변경 승인.
    **httpOnly를 흉내낼 수 없는 것은 MSW 제약이라 [데모 환경] 주석으로 명시**
- `test: 401 토큰 갱신과 재시도 스펙 추가`: pnpm test:run 으로 red 상태 직접 확인.
  예상된 "미구현" 케이스 7건 실패하고 기존 14건과 typecheck, lint는 통과.
- `feat: 401 토큰 갱신과 원요청 재시도 구현`: red 7건이 전부 green으로 전환되었고 3종 게이트 통과 확인.
  응답 본문의 경계 캐스트 1곳은 주석 명시 조건으로 승인함.
- `test: 가상 스크롤 렌더 윈도우 스펙 추가`: 신규 12건 "미구현" 케이스 확인.
  중간 위치 계산은 직접 검산함. (scrollTop 10000 -> 시작 95, 오프셋 9500)
  미세 스크롤 테스트가 0 -> 30이 아니라 110 -> 150인 이유 확인. (0 -> 30은 endIndex 경계를 건너서 윈도우가 바뀜)
- `feat: 가상 스크롤 렌더 윈도우 계산과 훅 구현`: 12건 모두 green 전환과 3종 게이트 확인.
  isSameWindow 조건을 지워보고 미세 스크롤 테스트가 실패하는 것 까지 확인.
- `feat: 라우터 셸과 GNB 추가`: 로컬 브라우저에서 비로그인 리다이렉트 확인.
  리다이렉트 쿼리에 원래 경로 남는 것 확인. (/task 접근 시 ?redirect=%2Ftask).
  로그인 UI가 아직 없어서 로그인 상태는 테스트 4건으로 대신 확인.
- `feat: StyleSeed 규칙 기반 디자인 토큰 체계 도입`: 스타일 방향은 논의로 결정.
  StyleSeed는 규칙 체계만 참고하고 팔레트는 자체 구성 (README에 출처 명시). dev에서 canvas/surface 분리, 현재 페이지 pill, hover
  transition 확인. GNB는 스크린샷 놓고 로고 추가, 크기 위계, 로그인 버튼 축소까지 직접 주문해서 맞춤. STYLESEED.md 잠금 규칙 검토.
- `feat: 로그인 폼과 세션 추가`: /task에서 튕겨 로그인하면 /task로 복귀하는 것 확인. 
  이메일 형식 오류에 aria-invalid와 role=alert 연결되는 것 개발자 도구로 확인. 
  실패 모달은 Escape로 닫히고 포커스가 버튼으로 돌아옴. 쿠키에 token 확인.
  직접 화면 보면서 잡아낸 문제 3건
    1. 카드 좌상단 모서리가 깨져 보이는 것 발견. radius 토큰명이 Tailwind 내장 rounded-ss(좌상단 코너 유틸)와 충돌한다는 진단 확인 후 
      rounded-card로 개명 승인
    2. 로그인 카드가 아래로 밀려 보여서 위치 조정 지시. GNB 아래 영역 중앙이 아니라 화면 전체 기준 정중앙으로 보정
    3. 버튼에 pointer 커서가 안 나오는 것 발견. Tailwind v4 preflight 변경이 원인, 전역 복원 지시
- `feat: 대시보드 지표 표시 추가`: 로그인 후 지표 3개 값 검산 확인.  네트워크 throttle로 스켈레톤, offline으로 에러 상태와 다시 시도 복구까지 확인. 
  Query 재시도를 끈 이유 (401 재시도는 http 클라이언트 담당, 이중 재시도 방지) 확인.
- `feat: 할 일 목록과 무한 스크롤 추가`: 로그인 후 목록에서 스크롤로 다음 페이지 이어지는 것 확인, 500건까지 도달. 목록 영역만 스크롤되고 GNB와 제목은 고정.
  body 스크롤 때문에 무한 스크롤이 안 걸리던 버그는 에이전트가 브라우저로 확인하다 발견해서 h-dvh 레이아웃으로 수정한 것 확인. 
  jsdom 테스트가 이런 레이아웃 문제를 못 잡는다는 것도 같이 확인. 끝까지 내리면 DOM에 카드 500개 쌓이는 것 Elements로 확인.
  직접 화면 보면서 조정 지시한 것 4건
    1. TODO 뱃지 주황이 경고처럼 보여서 중립 그레이로 변경.
    2. 스크롤바가 카드 오른쪽을 덮어서 여백 확보 지시
    3. 기본 스크롤바가 너무 진해서 연한 그레이로 통일
    4. 메모가 두 줄 넘을 때 어떻게 되는지 보려고 시드를 늘려달라 함.
     두 줄에서 말줄임으로 잘리는 것 확인 (카드 높이 고정 유지)
- `feat: 목록에 가상 스크롤 적용`: 스크롤 중 querySelectorAll로 li 수 확인, 항상 17개 이하 (적용 전 500개). 빠른 스크롤에도 빈 화면 없이 이어짐.
  Performance 녹화로 스크롤 중 Layout 이벤트가 안 생기는 것 확인 (transform만 바뀜). 
  스크롤바 중간쯤에서 250번대 항목 나오는 것으로 offsetY 계산 맞는 것 확인.
  /code-review 로 알게 된 사실 (15건 지적 중 10건 반영 지시)
    1. 셀프 리뷰가 못 잡은 실버그 1건: 에러 화면 복구로 컨테이너가 재마운트되면 scrollTop은 0인데 훅의 ref에는 이전 위치가 남아 빈 화면이 되는 문제.
      수정 확인했고, 리뷰어 제안 코드만으로는 setState bail-out 때문에 부족해서 forceRender까지 추가된 것도 재마운트 테스트로 확인
    2. 접근성 3건 반영 확인: 무한 스크롤 중 aria-setsize는 -1(총수 미상), preflight가 리스트 시맨틱을 지워서 role="list" 명시, 스크롤 영역 키보드 접근
    3. 테스트 공백 보완: 기존 스윕이 "너무 많이 렌더"만 막고 "너무 적게 렌더"(빈 화면 회귀)를 안 막던 것에 커버리지 불변식 추가. floor/ceil 바꿔보면 이제 실패함
    4. 나머지 5건(rAF 스로틀, ResizeObserver 등)은 트레이드오프로 README 한계에 기록하는 걸로 결정
- `feat: 할 일 상세 조회 추가`: 목록에서 카드 눌러 상세 진입, 제목과 등록 시각, 메모 확인. 5번처럼 메모가 긴 항목이 목록 카드에서는 두 줄로 잘리고 상세에서는 전문이 보이는 것 확인.
  상세 응답에 status가 없어서 TODO/DONE 뱃지를 상세에 못 넣는 것은 openapi에서 직접 확인. 404가 아닌 실패는 브라우저에서 500을 재현할 방법이 마땅치 않아 테스트로 확인하고,
  404 조건을 지웠을 때 그 테스트가 깨지는 것까지 봄. 
  직접 검증하면서 잡아낸 것 2건
    1. 커밋 계획이 상세 조회와 삭제를 한 뭉텅이로 묶고 있어서 3개로 다시 쪼개라고 지시. 상세 조회, 삭제 확인 모달, 목록 스크롤 복원 순서로 분리
    2. 404 화면을 보려고 /task/9999를 주소창에 넣었더니 로그인 페이지로 튕김. 에이전트는 access token 메모리 보관의 결과라며 관찰로만 남기고 범위 밖으로 뒀는데,
      새로고침과 딥링크는 평가자가 반드시 밟는 경로라 그냥 둘 수 없다고 보고 원인 규명 지시. 로그인 상태에서 정상 경로 /task를 새로고침해도 같은 증상이고 refresh 쿠키는 살아 있음.
      부팅 시점에는 API 요청이 없어 401이 안 나고, 그래서 반응형 갱신이 안 걸리는 구조. 요구사항 밖이지만 범위에 넣기로 결정. 상세 조회 커밋 직후 별도 커밋으로 처리
- `feat: 부팅 시 세션 복원 추가`: 로그인 후 새로고침해도 그 페이지에 머무는 것, /task/5 딥링크가 로그인 거치지 않고 바로 열리는 것 확인.
  쿠키를 지우면 예전대로 로그인으로 튕기고 redirect 쿼리도 그대로인 것 확인. Network에서 첫 로드에 POST /api/refresh가 렌더 전 1회만 나가는 것 확인.
- `feat: 할 일 삭제 확인 모달 추가`: 모달에서 다른 번호를 넣으면 제출 버튼이 비활성화 상태로 유지되고 정확한 번호를 입력했을 때만 활성화 되는 것 확인. 
  제출하면 목록으로 이동하게 안내가 뜨며 해당 항목이 빠지는 것 확인. 두 건 지우고 대시보드가 498/333/165로 바뀌는 것 직접 검산 (원래 500/334/166, TODO 하나와 DONE 하나 삭제).
  모달 Tab 순환과 Escape 닫기, 닫은 뒤 삭제 버튼으로 포커스 복귀 확인. 안내가 5초 뒤 사라질 때 목록 영역이 늘어나는 것은 확인하고 그대로 두기로 함.
  처음 만든 인라인 배너가 사라질 때 목록이 밀려 올라오는 게 거슬려서 상단 중앙 토스트로 바꾸라고 지시. shadcn 도입은 Radix와 CVA가 딸려오고 자체 토큰 체계와 충돌해서 반려하고 직접 구현으로 진행.
  토스트가 뜨고 사라져도 목록이 안 움직이는 것과 닫기 버튼 동작 확인.
- `feat: 상세에 목록 복귀 버튼 추가`: 상세에서 목록으로 돌아갈 방법이 브라우저 뒤로가기뿐인 것을 발견해 보완 지시. 딥링크로 새 탭에 열었을 때 히스토리가 비어 탈출구가 GNB뿐이라는 설명 확인. 목록으로 클릭 시 이동 확인.
  배치는 세 번 조정 지시함 (제목 위 텍스트 링크 -> 삭제 버튼 좌측, 링크 -> 일반 버튼 모양, ArrowLeft -> ChevronLeft).
  버튼이 위젯 성공 분기에 들어가면서 로딩과 에러 화면에는 복귀 수단이 없어진 것 확인하고, 404는 자체 복귀 버튼이 있고 GNB도 살아 있어 그대로 두기로 함.
- `feat: 목록 스크롤 위치 복원 추가`: 목록에서 아래로 깊이 내려간 뒤 상세로 이동 후 뒤로가기 하면 기존 위치로 돌아오는 것 확인. GNB로 새로 들어오면 최상단인 것 확인.
  삭제 후에도 이전 위치 근처로 돌아오고, 그 상태에서 뒤로가기를 눌러도 지운 항목의 404가 뜨지 않는 것 확인. 새로고침하면 최상단으로 가는 것도 확인.
  처음 제안은 모듈 변수로 모든 진입에서 복원하는 안이었는데, 메뉴로 새로 들어오면 처음부터 보여주는 게 맞지 않은지 재검토시킴. 브라우저 기본 동작과 라우터 규약이 히스토리 기준이라는 근거를 받고 방향 전환에 동의.
  에이전트가 중간에 복원이 안 된다고 보고했는데 직접 보니 되고 있었고, 1페이지만 올라온 상태에서 잰 값이라는 것으로 정정받음.
- `feat: 회원정보 표시 추가`: GNB로 회원정보 페이지에 진입해 이름과 메모가 나오는 것 확인. Network의 /api/user 응답과 화면 값이 같은 것 확인.
- `feat: 로그아웃 추가`: 요구사항에 없는 기능이라 넣을지 먼저 물었고, 부팅 복원을 넣은 뒤로 명세된 비로그인 GNB 상태를 개발자 도구 없이는 볼 수 없게 됐다는 근거를 받고 범위에 넣기로 함. 
  버튼 위치는 상세의 삭제 버튼과 같은 자리로 지정. 로그아웃 후 GNB가 로그인으로 바뀌고 쿠키가 지워지는 것 확인. 그 상태에서 /task 접근 시 redirect 쿼리와 함께 로그인으로 가는 것,
  새로고침해도 세션이 되살아나지 않는 것 확인. 요구사항 밖 기능을 넣은 이유는 README 설계 결정에 남기도록 지시.